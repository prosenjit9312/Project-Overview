apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demo-pod.fullname" . }}-test-connection"
  labels:
    {{- include "demo-pod.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demo-pod.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
